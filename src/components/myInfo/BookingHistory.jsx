import React from "react";
import styled from "styled-components";

const BookingHistory = () => {
  return <Wrapper>booking-history</Wrapper>;
};

export default BookingHistory;

const Wrapper = styled.div`
  width: 100%;
  border: 2px solid #ececec;
  padding: 2rem 1rem;
`;
