import React from "react";
import styled from "styled-components";

const EditMyInfo = () => {
  return <Wrapper>edit-info</Wrapper>;
};

export default EditMyInfo;

const Wrapper = styled.div`
  width: 100%;
  border: 2px solid #ececec;
  padding: 2rem 1rem;
`;
